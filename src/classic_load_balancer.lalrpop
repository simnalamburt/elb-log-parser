use std::str::FromStr;

grammar;

// TODO: Implement Classic LB v1 and v2 log formats
pub Term: i32 = {
    <n:Num> => n,
    "(" <t:Term> ")" => t,
};

Num: i32 = <s:r"[0-9]+"> => i32::from_str(s).unwrap();
